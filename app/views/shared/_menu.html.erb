<main class="container bg-black mai-cont p-0">
  <nav class="navbar navbar-expand-lg navbar-light m-0 p-0 sticky-top">
    <div class="container nav p-0 ">
      <button
        id="navbar-toggler-button"
        class="navbar-toggler d-lg-none"
        type="button"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>

      <!-- Arrow button -->
      <h3 class="navbar-brand text-white m-0 p-0"><%= @page_name %></h3>
      <a href="javascript:history.back();" class="back-arrow d-lg-none text-white">
        <i class="fas fa-arrow-left"></i>
      </a>
    </div>

    <div class="collapse navbar-collapse justify-content-start " id="navbarNav">
      <!-- Add the overlay element -->
     
      <div class=" navbar-nav ml-auto p-0 row-avata row d-flex justify-content-start align-items-center">
      <div class="overlay" id="overlay">
      <!-- Close (X) icon -->
      <i class="fas fa-times close-icon white" id="close-icon"></i>
    </div>
        <div class=" avatar row align-items-center justify-items-center">
          <!-- Add Font Awesome avatar image -->
          <i class=" avatar-img fas fa-user-circle fa-6x text-primary mt-3 ml-auto p-0"></i>
          <%= user.name %>
        </div>
        <ul class="item-nav">
          <li class="nav-item">
            <a class="nav-link text-white" href="#">Account Settings</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-white" href="#">Category</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-white" href="#">Transaction</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-white" href="#">Contact</a>
          </li>
        </ul>
      </div>
      <li class="nav-itm">
        <!-- Logout button -->
        <%= form_tag(destroy_user_session_path, method: :delete) do %>
          <%= submit_tag('Log Off', class: 'btn btn-danger') %>
        <% end %>
      </li>
    </div>
  </nav>
</main>
